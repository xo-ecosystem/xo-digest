repos:
  - repo: local
    hooks:
      - id: dev-cleanup
        name: Dev Cleanup (tmp, .cursor, .history, etc)
        entry: bash scripts/dev_cleanup.sh
        language: system
        always_run: true
